# 🚀 تعليمات التثبيت السريع

## خطوة 1: إنشاء قاعدة البيانات

### طريقة 1 - من خلال phpMyAdmin:
1. افتح phpMyAdmin
2. انقر على "جديد" (New) لإنشاء قاعدة بيانات
3. اختر علامة التبويب "استيراد" (Import)
4. اختر ملف `database.sql`
5. انقر "تنفيذ" (Go)

### طريقة 2 - من خلال سطر الأوامر:
```bash
# إنشاء قاعدة البيانات
mysql -u root -p -e "CREATE DATABASE book_platform CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# استيراد الجداول
mysql -u root -p book_platform < database.sql
```

## خطوة 2: تكوين الإعدادات

افتح ملف `config.php` وتأكد من صحة إعدادات قاعدة البيانات:

```php
define('DB_HOST', 'localhost');     // عنوان السيرفر
define('DB_NAME', 'book_platform'); // اسم قاعدة البيانات
define('DB_USER', 'root');          // اسم المستخدم
define('DB_PASS', '');              // كلمة المرور
```

## خطوة 3: ضبط الصلاحيات

### على Linux/Mac:
```bash
chmod -R 755 uploads/
chmod -R 755 uploads/books/
chmod -R 755 uploads/audio/
chmod -R 755 uploads/covers/
chmod -R 755 uploads/qr/
```

### على Windows:
تأكد من أن مجلدات `uploads` وجميع المجلدات الفرعية لديها صلاحيات الكتابة.

## خطوة 4: استيراد الكتب الموجودة (اختياري)

إذا كان لديك كتب في مجلد `books/`، يمكنك استيرادها:

```bash
php import-existing-books.php
```

## خطوة 5: الوصول للمنصة

افتح المتصفح وانتقل إلى:
```
http://localhost/german-books
```

## 🔐 الحسابات التجريبية

### أدمن:
- البريد: admin@bookplatform.com
- كلمة المرور: admin123

### قارئ:
- البريد: reader@bookplatform.com
- كلمة المرور: reader123

## ⚠️ ملاحظات مهمة

1. **تغيير كلمات المرور**: قم بتغيير كلمات المرور الافتراضية فوراً!

2. **HTTPS في الإنتاج**: تأكد من تفعيل HTTPS على السيرفر الحقيقي

3. **النسخ الاحتياطي**: قم بعمل نسخة احتياطية دورية لقاعدة البيانات والملفات

4. **الأمان**:
   - لا تستخدم `root` في الإنتاج
   - قم بتأمين ملف `config.php`
   - فعّل HTTPS

## 🐛 حل المشاكل الشائعة

### المشكلة: لا يمكن رفع الملفات
**الحل**: تأكد من:
```php
// في php.ini
upload_max_filesize = 50M
post_max_size = 50M
```

### المشكلة: خطأ في الاتصال بقاعدة البيانات
**الحل**: تحقق من:
- تشغيل MySQL
- صحة بيانات الاتصال في config.php
- وجود قاعدة البيانات

### المشكلة: PDF لا يظهر
**الحل**: تأكد من:
- وجود الملف في مجلد uploads/books/
- صلاحيات القراءة على الملف
- عمل JavaScript في المتصفح

## 📞 الدعم

إذا واجهت أي مشكلة:
1. تحقق من ملف `error.log`
2. تأكد من جميع المتطلبات
3. راجع هذا الملف

---

✅ بعد إكمال جميع الخطوات، المنصة جاهزة للاستخدام!
