# 🚨 حل مشكلة "This Page Does Not Exist"

## المشكلة
عند فتح `login.php` أو `admin/` تحصل على:
```
❌ This Page Does Not Exist
❌ 404 Not Found
❌ Page Not Available
```

---

## ✅ الحل السريع (3 خطوات)

### الخطوة 1: اختبار السيرفر 🔍

**ارفع وافتح:**
```
https://netlabacademy.com/test-server.php
```

**كلمة المرور:** `test123`

**ماذا ستفعل هذه الأداة؟**
- ✅ تفحص جميع الملفات المطلوبة (login.php, admin/, reader/, إلخ)
- ✅ تفحص الصلاحيات الحالية
- ✅ تفحص اتصال قاعدة البيانات
- ✅ تكتشف المشكلة بالضبط

**النتيجة:**
- إذا كانت الملفات موجودة → انتقل للخطوة 2
- إذا كانت الملفات مفقودة → ارفعها أولاً (راجع `UPLOAD_GUIDE.md`)

---

### الخطوة 2: إصلاح الصلاحيات 🔧

**ارفع وافتح:**
```
https://netlabacademy.com/fix-permissions.php
```

**كلمة المرور:** `fix123`

**ماذا ستفعل هذه الأداة؟**
- ✅ تضبط صلاحيات جميع ملفات PHP إلى `644`
- ✅ تضبط صلاحيات المجلدات إلى `755`
- ✅ تنشئ مجلد uploads إذا لم يكن موجوداً
- ✅ تصلح صلاحيات admin/ و reader/

**بعد الإصلاح:**
```bash
# احذف الملف فوراً!
rm fix-permissions.php
```

**اختبر الآن:**
```
https://netlabacademy.com/login.php
```

إذا عمل → **تم الحل! 🎉**
إذا لم يعمل → انتقل للخطوة 3

---

### الخطوة 3: إصلاح .htaccess ⚙️

**المشكلة:** ملف `.htaccess` يحتوي على قواعد غير متوافقة

**الحل عبر cPanel File Manager:**

1. افتح **File Manager**
2. اذهب إلى `public_html/`
3. ابحث عن ملف `.htaccess`
4. كليك يمين → **Rename** → `.htaccess.backup`
5. ابحث عن ملف `.htaccess-simple`
6. كليك يمين → **Rename** → `.htaccess`

**أو عبر SSH:**
```bash
mv .htaccess .htaccess.backup
mv .htaccess-simple .htaccess
```

**اختبر الآن:**
```
https://netlabacademy.com/login.php
```

**يجب أن يعمل الآن! ✅**

---

## 🎯 ملخص سريع

```
المشكلة: login.php لا يعمل

1️⃣ ارفع test-server.php → اكتشف المشكلة
   كلمة المرور: test123

2️⃣ ارفع fix-permissions.php → أصلح الصلاحيات
   كلمة المرور: fix123

3️⃣ إذا لم يعمل → استبدل .htaccess بـ .htaccess-simple

4️⃣ احذف الملفات:
   rm test-server.php fix-permissions.php
```

---

## 📋 الأسباب المحتملة

### 1️⃣ الملفات لم يتم رفعها
**الحل:**
- راجع `UPLOAD_GUIDE.md`
- استخدم `UPLOAD_CHECKLIST.txt`

### 2️⃣ الصلاحيات خاطئة
**الحل:**
- استخدم `fix-permissions.php`
- أو يدوياً: ملفات=644، مجلدات=755

### 3️⃣ .htaccess غير متوافق
**الحل:**
- استخدم `.htaccess-simple` بدلاً منه

### 4️⃣ الموقع في مجلد فرعي
**الحل:**
- تأكد أن الملفات في `/public_html/` وليس `/public_html/subfolder/`

### 5️⃣ Apache mod_rewrite معطل
**الحل:**
- تواصل مع الاستضافة لتفعيل `mod_rewrite`

---

## 🔍 فحص يدوي

### عبر cPanel File Manager:

**1. تحقق من وجود الملفات:**
```
✅ /public_html/login.php
✅ /public_html/index.php
✅ /public_html/admin/dashboard.php
✅ /public_html/reader/dashboard.php
```

**2. تحقق من الصلاحيات:**
- كليك يمين على `login.php` → **Change Permissions**
- يجب أن تكون: `644` أو `rw-r--r--`

**3. تحقق من المجلدات:**
- كليك يمين على `admin/` → **Change Permissions**
- يجب أن تكون: `755` أو `rwxr-xr-x`

---

## 🆘 إذا استمرت المشكلة

### تعطيل .htaccess مؤقتاً:
```bash
mv .htaccess .htaccess.disabled
```

**ثم اختبر:** إذا عمل الموقع → المشكلة من .htaccess

### فحص error.log:
```bash
tail -100 error_log
# أو
tail -100 /var/log/apache2/error.log
```

### تفعيل عرض الأخطاء (مؤقتاً):
في `config.php`:
```php
ini_set('display_errors', 1);
error_reporting(E_ALL);
```

---

## ✅ قائمة التحقق

- [ ] رفعت جميع الملفات (استخدم UPLOAD_CHECKLIST.txt)
- [ ] شغّلت test-server.php ← اكتشفت المشكلة
- [ ] شغّلت fix-permissions.php ← أصلحت الصلاحيات
- [ ] استبدلت .htaccess بـ .htaccess-simple (إذا لزم)
- [ ] login.php يعمل الآن ✅
- [ ] admin/dashboard.php يعمل ✅
- [ ] حذفت test-server.php و fix-permissions.php ✅

---

## 🎉 بعد الحل

**1. تثبيت قاعدة البيانات:**
```
https://netlabacademy.com/install-simple.php
```

**2. حذف ملفات التثبيت:**
```bash
rm install.php install-simple.php test-server.php fix-permissions.php
```

**3. تسجيل الدخول:**
```
https://netlabacademy.com/login.php

Admin:
البريد: admin@bookplatform.com
كلمة المرور: admin123
```

---

**💡 نصيحة:** استخدم `test-server.php` أولاً دائماً لاكتشاف المشكلة بدقة!
