# 📤 دليل رفع الملفات إلى netlabacademy.com

## 🎯 المشكلة الحالية

عندما تفتح `https://netlabacademy.com/login.php` تحصل على:
```
This Page Does Not Exist
```

**السبب:** الملفات موجودة فقط على جهازك المحلي ولم يتم رفعها إلى السيرفر!

---

## 📋 قائمة الملفات والمجلدات المطلوب رفعها

### ✅ ملفات الجذر (Root):
```
/
├── config.php               ⚠️ (عدّل بيانات قاعدة البيانات أولاً)
├── auth.php
├── login.php               ← المطلوب!
├── logout.php
├── index.php
├── play-audio.php
├── sitemap.php
├── seo-functions.php
├── database.sql
├── install.php             ← شغّله مرة واحدة ثم احذفه
├── install-simple.php      ← بديل للتثبيت
├── import-existing-books.php
├── .htaccess              ⚠️ (مهم للأمان)
├── robots.txt
└── README.md
```

### ✅ مجلدات (Folders):
```
/admin/                    ← جميع الملفات داخل هذا المجلد
/reader/                   ← جميع الملفات داخل هذا المجلد
/uploads/                  ← أنشئ المجلد وصلاحياته 755
  ├── books/              ← 755
  ├── audio/              ← 755
  ├── covers/             ← 755
  └── qr/                 ← 755
```

---

## 🚀 طريقة الرفع (3 خيارات)

### الطريقة 1: عبر cPanel File Manager (الأسهل) ⭐

#### الخطوة 1: تسجيل الدخول
1. اذهب إلى: `https://netlabacademy.com/cpanel`
2. سجّل دخول بحسابك

#### الخطوة 2: فتح File Manager
1. ابحث عن **File Manager**
2. اضغط عليه
3. اذهب إلى مجلد **public_html**

#### الخطوة 3: رفع الملفات
**خيار أ: رفع كل الملفات كـ ZIP (موصى به)**
1. على جهازك، اضغط كليك يمين على مجلد المشروع
2. اختر **Compress to ZIP**
3. سمّيه: `book-platform.zip`
4. في cPanel File Manager:
   - اضغط **Upload**
   - اختر ملف `book-platform.zip`
   - انتظر حتى ينتهي الرفع
5. كليك يمين على الملف المرفوع → **Extract**
6. احذف ملف ZIP بعد فك الضغط

**خيار ب: رفع الملفات فرادى**
1. اضغط **Upload** في File Manager
2. اختر جميع الملفات والمجلدات
3. انتظر حتى ينتهي الرفع

#### الخطوة 4: ضبط الصلاحيات
1. كليك يمين على مجلد **uploads** → **Change Permissions**
2. اختر: `755` أو `drwxr-xr-x`
3. كرر لجميع المجلدات الفرعية:
   - uploads/books/
   - uploads/audio/
   - uploads/covers/
   - uploads/qr/

---

### الطريقة 2: عبر FTP (FileZilla)

#### الخطوة 1: معلومات الاتصال
احصل على بيانات FTP من cPanel:
- **Host:** ftp.netlabacademy.com أو netlabacademy.com
- **Username:** [اسم مستخدم cPanel]
- **Password:** [كلمة مرور cPanel]
- **Port:** 21

#### الخطوة 2: الاتصال
1. افتح FileZilla
2. File → Site Manager
3. New Site:
   - Protocol: FTP
   - Host: netlabacademy.com
   - Port: 21
   - User & Password
4. Connect

#### الخطوة 3: الرفع
1. في الجانب الأيسر (Local): افتح مجلد المشروع على جهازك
2. في الجانب الأيمن (Remote): افتح `/public_html/`
3. حدد جميع الملفات من اليسار
4. اسحبها إلى اليمين
5. انتظر حتى ينتهي الرفع (قد يستغرق دقائق)

#### الخطوة 4: التحقق
في FileZilla، يجب أن ترى:
```
/public_html/
  ├── admin/
  ├── reader/
  ├── uploads/
  ├── login.php          ← تحقق من وجوده!
  ├── index.php
  ├── config.php
  └── ...
```

---

### الطريقة 3: عبر SSH/Terminal (للمحترفين)

```bash
# 1. ضغط المشروع محلياً
cd /home/user/german-books
tar -czf book-platform.tar.gz .

# 2. رفع عبر SCP
scp book-platform.tar.gz username@netlabacademy.com:/home/username/public_html/

# 3. الاتصال عبر SSH
ssh username@netlabacademy.com

# 4. فك الضغط
cd public_html
tar -xzf book-platform.tar.gz
rm book-platform.tar.gz

# 5. ضبط الصلاحيات
chmod 755 uploads/
chmod 755 uploads/books/
chmod 755 uploads/audio/
chmod 755 uploads/covers/
chmod 755 uploads/qr/
```

---

## ⚠️ تحذيرات مهمة

### 1. تحديث config.php قبل الرفع!
قبل رفع الملفات، **تأكد** من تعديل `config.php`:

```php
// ✅ الإعدادات الصحيحة للإنتاج
define('DB_HOST', 'localhost');
define('DB_NAME', 'u186120816_books');
define('DB_USER', 'u186120816_minaboulesf3');
define('DB_PASS', 'yd+I*aN6');
define('SITE_URL', 'https://netlabacademy.com');

// ❌ لا تنسى تعطيل وضع التطوير
define('DEBUG_MODE', false);  // مهم!
```

### 2. رفع ملف .htaccess
تأكد من رفع `.htaccess` (يبدأ بنقطة!)
- في Windows قد يكون مخفياً
- في FileZilla: Server → Force showing hidden files

### 3. التأكد من رفع جميع المجلدات
```
✅ admin/          (يجب رفعه كاملاً)
✅ reader/         (يجب رفعه كاملاً)
✅ uploads/        (أنشئه وأعطه صلاحية 755)
```

### 4. ملف database.sql
- ارفعه مع بقية الملفات
- سيستخدمه install.php لإنشاء قاعدة البيانات

---

## 🧪 التحقق من نجاح الرفع

### 1. افتح هذه الصفحات:
```
✅ https://netlabacademy.com/
   → يجب أن تظهر الصفحة الرئيسية

✅ https://netlabacademy.com/login.php
   → يجب أن تظهر صفحة تسجيل الدخول (وليس "Page Does Not Exist")

✅ https://netlabacademy.com/install.php
   → يجب أن تظهر صفحة التثبيت
```

### 2. إذا رأيت أخطاء PHP:
```php
// في config.php، عطّل عرض الأخطاء:
ini_set('display_errors', 0);
error_reporting(0);
```

### 3. إذا رأيت 404 Not Found:
- تحقق من أن الملفات في `/public_html/` وليس في مجلد فرعي
- تحقق من أسماء الملفات (حساسة لحالة الأحرف)

---

## 📊 بعد رفع الملفات بنجاح

### الخطوة 1: تثبيت قاعدة البيانات
```
1. افتح: https://netlabacademy.com/install-simple.php
2. اضغط "🚀 تثبيت تلقائي"
3. انتظر النتيجة: ✅ تم التثبيت بنجاح!
```

### الخطوة 2: حذف ملفات التثبيت (مهم!)
```bash
# عبر File Manager أو SSH
rm install.php
rm install-simple.php
```

### الخطوة 3: تسجيل الدخول
```
افتح: https://netlabacademy.com/login.php

Admin:
البريد: admin@bookplatform.com
كلمة المرور: admin123

Reader:
البريد: reader@bookplatform.com
كلمة المرور: reader123
```

---

## 🆘 حل المشاكل

### المشكلة: "This Page Does Not Exist"
**الحل:**
- الملفات لم تُرفع بعد → ارفعها!
- الملفات في المكان الخطأ → تأكد أنها في `/public_html/`

### المشكلة: "500 Internal Server Error"
**الحل:**
- تحقق من صلاحيات الملفات (644 للملفات، 755 للمجلدات)
- تحقق من ملف `.htaccess` (قد يكون غير متوافق)

### المشكلة: "Database Connection Error"
**الحل:**
- تحقق من بيانات `config.php`
- تأكد أن قاعدة البيانات موجودة في cPanel

### المشكلة: "Permission Denied"
**الحل:**
```bash
chmod 755 uploads/
chmod 755 uploads/*
```

---

## ✅ قائمة التحقق النهائية

- [ ] تم رفع جميع ملفات الجذر (login.php, index.php, config.php, إلخ)
- [ ] تم رفع مجلد `/admin/` بالكامل
- [ ] تم رفع مجلد `/reader/` بالكامل
- [ ] تم إنشاء مجلد `/uploads/` مع المجلدات الفرعية
- [ ] تم ضبط صلاحيات uploads إلى 755
- [ ] تم تحديث `config.php` ببيانات الإنتاج
- [ ] تم رفع ملف `.htaccess`
- [ ] تم رفع ملف `database.sql`
- [ ] `https://netlabacademy.com/login.php` يعمل ✅
- [ ] تم تشغيل install.php بنجاح
- [ ] تم حذف ملفات التثبيت
- [ ] تم تسجيل الدخول بنجاح

---

## 🎉 الآن يمكنك استخدام المنصة!

بعد رفع الملفات وتثبيت قاعدة البيانات:
- ✅ `https://netlabacademy.com/login.php` ← سيعمل!
- ✅ `https://netlabacademy.com/` ← الصفحة الرئيسية
- ✅ `https://netlabacademy.com/admin/dashboard.php` ← لوحة الأدمن
- ✅ `https://netlabacademy.com/reader/dashboard.php` ← لوحة القارئ

---

**💡 نصيحة:** استخدم FileZilla أو cPanel File Manager، هما الأسهل!
