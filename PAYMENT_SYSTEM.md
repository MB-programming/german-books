# 💰 نظام الدفع والشراء

## نظرة عامة

تم إضافة نظام دفع متكامل للكتب المدفوعة يتيح للأدمن تفعيل الدفع على الكتب، وللقراء شراء الكتب عبر طرق دفع محلية مع موافقة الأدمن.

## 🎯 المميزات

### للأدمن:

1. **إضافة كتب مدفوعة**:
   - تحديد السعر عند رفع الكتاب
   - تفعيل/إلغاء تفعيل وضع الدفع

2. **إدارة طلبات الشراء**:
   - عرض جميع الطلبات (معلقة، مقبولة، مرفوضة)
   - قبول أو رفض الطلبات مع إضافة ملاحظات
   - إحصائيات شاملة للطلبات

3. **تتبع المدفوعات**:
   - عرض تفاصيل كل طلب
   - معلومات طريقة الدفع المستخدمة
   - تاريخ الطلبات والموافقات

### للقارئ:

1. **عرض واضح للأسعار**:
   - الكتب المجانية تظهر بوضوح
   - الكتب المدفوعة تظهر بالسعر

2. **عملية شراء سهلة**:
   - اختيار طريقة الدفع (إنستاباي أو فودافون كاش)
   - إرسال طلب عبر واتساب تلقائياً
   - تتبع حالة الطلب

3. **حماية المحتوى**:
   - لا يمكن قراءة الكتب المدفوعة بدون شراء
   - التحويل التلقائي لصفحة الشراء

## 💳 طرق الدفع المدعومة

### 1. إنستاباي (InstaPay)
- **الرقم**: 01222112819
- **الأيقونة**: 💳

### 2. فودافون كاش (Vodafone Cash)
- **الرقم**: 01014959132
- **الأيقونة**: 📱

### 3. التواصل عبر واتساب
- **الرقم**: 01222112819
- يتم إرسال تفاصيل الطلب تلقائياً عبر واتساب

## 📋 خطوات الشراء (للقارئ)

1. **اختيار الكتاب**:
   - تصفح الكتب في لوحة التحكم
   - الكتب المدفوعة تظهر بالسعر

2. **اختيار طريقة الدفع**:
   - الضغط على زر "شراء الكتاب"
   - اختيار طريقة الدفع المناسبة

3. **التواصل عبر واتساب**:
   - الضغط على زر "إرسال طلب عبر واتساب"
   - يتم فتح محادثة واتساب مع رسالة جاهزة تحتوي على:
     * عنوان الكتاب
     * السعر
     * اسم المستخدم
     * البريد الإلكتروني
     * طريقة الدفع المختارة
     * الرقم المطلوب التحويل عليه

4. **تحويل المبلغ**:
   - إرسال الرسالة عبر واتساب
   - تحويل المبلغ عبر الطريقة المختارة
   - إرسال لقطة شاشة لعملية التحويل

5. **انتظار الموافقة**:
   - يظهر للمستخدم "طلب معلق"
   - ينتظر موافقة الأدمن
   - بعد الموافقة يمكنه قراءة الكتاب مباشرة

## 🔧 إدارة الطلبات (للأدمن)

### عرض الطلبات

**الوصول**: لوحة تحكم الأدمن → طلبات الشراء

**المعلومات المعروضة**:
- معلومات المستخدم (الاسم، البريد الإلكتروني)
- عنوان الكتاب
- المبلغ
- طريقة الدفع
- رقم الهاتف
- تاريخ الطلب
- الحالة (معلق، مقبول، مرفوض)

### قبول طلب

1. الضغط على زر "قبول"
2. إضافة ملاحظات (اختياري)
3. تأكيد القبول
4. يتم تلقائياً:
   - تحديث حالة الطلب إلى "مقبول"
   - إضافة سجل في جدول المشتريات
   - السماح للمستخدم بقراءة الكتاب

### رفض طلب

1. الضغط على زر "رفض"
2. إضافة ملاحظات (اختياري)
3. تأكيد الرفض
4. يتم تحديث حالة الطلب إلى "مرفوض"

## 🗄️ قاعدة البيانات

### جدول الكتب (تحديث)
```sql
ALTER TABLE books ADD COLUMN is_paid TINYINT(1) DEFAULT 0;
ALTER TABLE books ADD COLUMN price DECIMAL(10,2) DEFAULT 0.00;
```

### جدول طلبات الشراء
```sql
CREATE TABLE purchase_requests (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    book_id INT NOT NULL,
    payment_method ENUM('instapay', 'vodafone_cash') NOT NULL,
    amount DECIMAL(10,2) NOT NULL,
    phone_number VARCHAR(20),
    transaction_id VARCHAR(100),
    status ENUM('pending', 'approved', 'rejected') DEFAULT 'pending',
    request_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    admin_response_date TIMESTAMP NULL,
    admin_id INT,
    admin_notes TEXT,
    ...
);
```

### جدول المشتريات المكتملة
```sql
CREATE TABLE purchases (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    book_id INT NOT NULL,
    purchase_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    amount_paid DECIMAL(10,2) NOT NULL,
    payment_method ENUM('instapay', 'vodafone_cash') NOT NULL,
    purchase_request_id INT,
    ...
);
```

## 📱 تكامل واتساب

### الرسالة التلقائية

عند اختيار طريقة الدفع والضغط على زر واتساب، يتم توليد رسالة تحتوي على:

```
السلام عليكم،

أريد شراء الكتاب التالي:

📚 *عنوان الكتاب:* [اسم الكتاب]
💰 *السعر:* [السعر] جنيه
👤 *اسم المستخدم:* [الاسم]
📧 *البريد الإلكتروني:* [البريد]
📱 *رقم الهاتف:* [الرقم]

💳 *طريقة الدفع المختارة:* [إنستاباي/فودافون كاش]
🔢 *الرقم:* [رقم الحساب]

سأقوم بتحويل المبلغ الآن وإرسال لقطة شاشة لعملية التحويل.

شكراً لكم.
```

## 🔒 الأمان

### حماية المحتوى
- التحقق من الشراء قبل السماح بالقراءة
- إعادة توجيه تلقائية للكتب غير المشتراة
- منع الوصول المباشر للكتب المدفوعة

### التحقق من الصلاحيات
- فقط الأدمن يمكنه قبول/رفض الطلبات
- التحقق من الجلسة في كل صفحة
- حماية ضد SQL Injection و XSS

## 📊 الإحصائيات

في صفحة طلبات الشراء، يعرض النظام:
- عدد الطلبات المعلقة
- عدد الطلبات المقبولة
- عدد الطلبات المرفوضة

## 🎨 تجربة المستخدم

### للقارئ:
- واجهة واضحة وسهلة
- ألوان مميزة للكتب المجانية والمدفوعة
- مؤشرات حالة الطلب (معلق، مقبول، مرفوض)
- رسائل توجيهية واضحة

### للأدمن:
- لوحة تحكم منظمة
- إحصائيات في لمحة سريعة
- نظام تصفية وترتيب للطلبات
- عرض تفاصيل كاملة لكل طلب

## 📝 ملاحظات مهمة

1. **التحويل اليدوي**: النظام لا يقوم بمعالجة الدفع تلقائياً، بل يعتمد على التحويل اليدوي والموافقة اليدوية

2. **التواصل المباشر**: يتم التواصل مع العملاء عبر واتساب للتأكد من صحة الدفع

3. **المرونة**: الأدمن لديه السيطرة الكاملة على قبول أو رفض أي طلب

4. **الشفافية**: جميع الطلبات مسجلة ويمكن مراجعتها في أي وقت

## 🚀 التحديثات المطلوبة

بعد رفع الملفات:

1. تشغيل ملف `database.sql` المحدث:
```bash
mysql -u root -p book_platform < database.sql
```

2. التأكد من صلاحيات المجلدات:
```bash
chmod 755 uploads/
chmod 755 uploads/books/
chmod 755 uploads/audio/
```

3. تحديث أرقام الهواتف في `reader/purchase-book.php` إذا لزم الأمر

## 🎯 الملفات الجديدة/المعدلة

### ملفات جديدة:
- `reader/purchase-book.php` - صفحة شراء الكتاب
- `admin/purchase-requests.php` - إدارة طلبات الشراء

### ملفات محدثة:
- `database.sql` - جداول جديدة وحقول محدثة
- `admin/upload-book.php` - إضافة حقول السعر
- `reader/dashboard.php` - عرض الكتب المدفوعة
- `reader/view-book.php` - التحقق من الشراء
- `admin/dashboard.php` - رابط طلبات الشراء
- `admin/books.php` - رابط طلبات الشراء
- `admin/add-audio.php` - رابط طلبات الشراء

---

**تم التطوير بواسطة Claude** ❤️
